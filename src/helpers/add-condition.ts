import {ValidatorInterface} from "../interfaces/validator.interface";
import {ConditionInterface} from "../interfaces/condition.interface";

export const addCondition = (instanciatedConstraint: ConditionInterface) => {

}
